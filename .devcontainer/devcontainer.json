{
	"image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye",
	"postCreateCommand": "sudo apt-get update && sudo apt-get install git && pip3 install -r requirements.txt && pip install -e .",
	"name": "Python 3"
}
